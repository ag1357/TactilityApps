# tactility-cmakelists-version: 1
cmake_minimum_required(VERSION 3.20)
if(NOT DEFINED ENV{TACTILITY_SDK_PATH})
 message(FATAL_ERROR "Set TACTILITY_SDK_PATH or build with ../../tactility.py")
endif()
get_filename_component(TACTILITY_SDK_PATH "$ENV{TACTILITY_SDK_PATH}" ABSOLUTE)
include("${TACTILITY_SDK_PATH}/TactilitySDK.cmake")
tactility_project_pre(cascadeterrace)
set(COMPONENTS main)
project(cascadeterrace)
tactility_project_post(cascadeterrace)
