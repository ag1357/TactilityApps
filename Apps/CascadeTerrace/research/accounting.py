#!/usr/bin/env python3
import json,ctypes as C,subprocess,pathlib,hashlib
from abi import ROOT,View,Result,Frame,Context
p=ROOT/'research/results';training=json.loads((p/'training.json').read_text());final=json.loads((p/'final-summary.json').read_text());game=json.loads((p/'game-regression.json').read_text());freeze=json.loads((p/'freeze.json').read_text());fail={}
for mode in ['B','C']:
 x=final[mode];reasons=[]
 if x['accuracy']['answer']<.9:reasons.append('exact answer below 90%')
 bad=[k for k,v in x['classes'].items() if v['correct']/v['n']<.8]
 if bad:reasons.append('required classes below 80%: '+', '.join(bad))
 if game['3' if mode=='B' else '4']['fact_hit']<120:reasons.append('Cascade regression below existing baseline')
 if x['counts']['unsupported_propositions'] or x['counts']['truth_leaks']:reasons.append('epistemic violation')
 if mode=='C' and x['accuracy']['answer']<=final['B']['accuracy']['answer']:reasons.append('learned model does not beat deterministic baseline')
 fail[mode]={'passed':not reasons,'reasons':reasons}
report={'qualification':'Q1 INCOMPLETE; Q2 PENDING PHYSICAL VALIDATION','starting_commit':freeze['starting_commit'],'implementation_checkpoint':freeze['implementation_checkpoint'],'default_mode':0,'promotion_gate':fail,'architecture_selected_for_production':None,'scope':'Research checkpoint; no general free-text cognition success claimed','memory':{'semantic_view_bytes_desktop':C.sizeof(View),'result_trace_bytes_desktop':C.sizeof(Result),'frame_bytes_desktop':C.sizeof(Frame),'context_bytes_desktop':C.sizeof(Context),'neural_parameters':training['parameters'],'neural_storage_bytes_int8':training['quantized_bytes'],'learned_inference_local_arrays_bytes':64*4+46*4,'input_feature_buffer_bytes':160*2,'max_embedding_additions':160*64,'head_macs_per_inference':46*64,'max_graph_depth':12,'max_facts':64,'max_trace_steps':64,'max_output_propositions':8,'legacy_authorized_view_bytes':10952,'experimental_bridge_static_workspace_bytes':10952+C.sizeof(View)+C.sizeof(Result),'existing_npc_persistent_bytes':5384,'new_persistent_neural_state_bytes':0,'context_persistence':'12-byte live context is ephemeral. Meaningful memory persists in original game journal, not neural weights or generated prose.','stack_note':'Compiler reports in stack-usage.txt; per-function values are not simultaneous peaks. Nested parser/features plus call frames roughly 3KB on desktop; physical high-water mark pending.','p4_residency_note':'External ELF code/model/rodata placement is controlled by Tactility loader. Do not assume model is memory mapped from SD. Budget full 101432 model bytes plus loader allocations until measured.'},'npc_storage_projections':{str(n):5384*n for n in [1,100,1000,10000]},'p4':json.loads((p/'p4-build.json').read_text()),'physical_measurements':None,'optional_experiments':{'LTC_AGI_V3':'NOT IMPLEMENTED. Single learned candidate loses to deterministic baseline; recurrence has no evidence of fixing lexical/syntax errors. No superiority claim.','AetherSparse':'Controlled isolated occurrence-plane benchmark completed; no full-runtime adoption.','AetherCore':'Architectural reuse of typed workspace, legal mask, bounded execution, evidence and verifier; no historical controller code dependency.','Jev':'Jev-inspired typed decision model only; no reproduction claim.'},'known_post_freeze_findings':['Unrecognized secondary relation sentinel CG_OTHER also matches legacy OTHER facts; gameplay recall can emit extra unrelated memories. Frozen experimental candidate retained as failed, not silently repaired from held-out outcomes.','Legacy adapter has no causal equations and does not manufacture them; game counterfactuals abstain.','Legacy object prose is truncated to 95 characters in adapter; source IDs have no complete name registry; temporal metadata absent.','Experimental modes do not commit free-text assertions/promises into game state. Original default keeps existing behavior.','No learned entity/referent/argument heads; binding is deterministic. No secondary-relation head, no calibrated uncertainty.','Finite synthetic templates and small AND causal graphs do not establish open-ended language or general intelligence.','Final world identities/graphs are disjoint; final surface templates share the test challenge bank, so final is world-held, not a separately authored blind-language benchmark.','Reported absence of unsupported propositions is limited to this authorized schema/oracle/verifier; it does not certify all conversational implications.','Query-only context state has no learned recurrence; post-save synthetic labels are not used as evidence of process persistence. Actual separate-process game logs provide that evidence.'], 'next_step':'Keep bounded executor as a research interface. First make game facts/events use complete canonical relation/entity/causal/source metadata, add independently authored language worlds and abstention tests, then test a contextual span/argument encoder. Do not scale the existing pooled classifier.'}
(p/'mission-summary.json').write_text(json.dumps(report,indent=2)+'\n')
(p/'stack-usage.txt').write_text(''.join(x.read_text() for x in (ROOT/'build').glob('*account.su')))
print(json.dumps({'gate':fail,'model_bytes':training['quantized_bytes'],'P4_ELF_bytes':report['p4']['binary_bytes']},indent=2))
